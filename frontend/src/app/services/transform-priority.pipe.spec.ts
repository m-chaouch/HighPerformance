import { TransformPriorityPipe } from './transform-priority.pipe';

describe('TransformPriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformPriorityPipe();
    expect(pipe).toBeTruthy();
  });
});
