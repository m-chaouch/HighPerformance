import { TransformRatingPipe } from './transform-rating.pipe';

describe('TransformRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
