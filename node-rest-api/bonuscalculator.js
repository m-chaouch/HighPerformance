//bonuscalculator.js
// Function to calculate the bonus salary for a salesman
function calculateBonus(salesAmount) {
    const OrderBonus = 0;
    const PerformanceBonus = 0;
    const totalbonus = OrderBonus + PerformanceBonus;
    return totalbonus;
}

// Export the calculateBonus function to make it accessible in other files
module.exports = calculateBonus;